{
  "name": "example-provider",
  "version": "0.1.0",
  "description": "Example provider extension for pi — replace with your actual API provider",
  "keywords": ["pi-package", "pi-provider", "example"],
  "license": "MIT",
  "pi": {
    "extensions": ["./index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*"
  }
}
